import { Scene } from 'phaser';

export default class GameplayResults extends Scene {
  constructor() {
    super({ key: 'SongSelect' });
    // constructing stuff
  }
  preload() {
    // preloading stuff
  }
  create() {
    // creating stuff
  }
  update() {
    // updating stuff
  }
}